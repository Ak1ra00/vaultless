# Partition table for the encrypted build (env:esp32dev-secure).
#
# Identical to partitions.csv except for the nvs_keys partition, which NVS
# encryption requires: it holds the XTS keys used to encrypt the nvs partition
# and must itself be flash-encrypted (the "encrypted" flag below).
#
# spiffs is shortened by 0x1000 to make room without moving any other offset,
# so the CI merge_bin offsets stay valid.
#
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x5000,
otadata,    data, ota,      0xe000,   0x2000,
app0,       app,  ota_0,    0x10000,  0x140000,
app1,       app,  ota_1,    0x150000, 0x140000,
spiffs,     data, spiffs,   0x290000, 0x15F000,
nvs_keys,   data, nvs_keys, 0x3EF000, 0x1000,   encrypted
coredump,   data, coredump, 0x3F0000, 0x10000,
